import type { DividerProps } from 'antd'

import { Divider } from 'antd'

type RLDividerProps = DividerProps

const RLDivider = (props: RLDividerProps) => <Divider {...props} />

export default RLDivider
